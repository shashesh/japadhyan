import { ChantScreen } from '@/features/chant/ChantScreen';

/** The app opens straight to chanting (docs/product/vision.md#the-core-loop). */
export default function Index() {
  return <ChantScreen />;
}
