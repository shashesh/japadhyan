{
  "name": "@japadhyan/sync-lab",
  "version": "0.1.0",
  "private": true,
  "description": "Headless PowerSync devices against the local sync stack (S4). Needs Docker; run with `npm run sync:test`",
  "type": "module",
  "scripts": {
    "test:stack": "vitest run",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@japadhyan/shared": "*",
    "@powersync/common": "2.3.0",
    "@powersync/node": "1.1.0",
    "@supabase/supabase-js": "2.117.2",
    "better-sqlite3": "13.0.3"
  },
  "devDependencies": {
    "@types/node": "^24",
    "typescript": "~6.0.3",
    "vitest": "^4.1.11"
  }
}
