{
  "name": "@japadhyan/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Platform-agnostic types, business logic and constants shared by all apps",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.11",
    "typescript": "~6.0.3",
    "vitest": "^4.1.11"
  },
  "dependencies": {
    "@noble/hashes": "2.4.0",
    "zod": "^4.6.5"
  }
}
